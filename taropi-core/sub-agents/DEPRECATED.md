# Deprecated

`sub-agents` 模块已废弃，不再继续迭代。

现有能力仍然保留：不得删除 `subagent` 工具及相关能力注册，已有调用方可以继续使用；但不再新增功能、扩展交互或进行常规优化。

## 原因

模型能力越来越强大，成本也越来越高。主 Agent 的并行工具调用速度已接近多个 Agent，同时不会产生 sub-agent 上下文缓存碎片化的问题，因此不再继续投入该模块。
